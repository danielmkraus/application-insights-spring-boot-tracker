spring:
  application-insights:
    tracker:
      exclude-packages:
        - org.sample.service.SampleService**
      include-packages:
        - org.sample.**
      enabled: true
logging:
  level:
    root: debug